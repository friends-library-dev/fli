```bash
$ fli # link all the things
$ flu # unlink all the things
$ fli . # link just for cwd pkg
$ flu . # ulink just for cwd pkg
$ fln # ? copy files into native dir, since RN hates symlinks? (if time)
```

```c

```
